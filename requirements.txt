# GitPython 3.1.20 is the last release with Python 3.6 support.
# Keeping it, since Python 3.6 is a hard dependency.
GitPython==3.1.20

# humanize 3.14.0 is the last release with Python 3.6 support.
# Keeping it, since Python 3.6 is a hard dependency.
humanize==3.14.0

PyGithub==1.55
PyYAML==6.0
SQLAlchemy==1.4.37